package ${package}.${moduleName}.service;

import com.lzl.base.IBaseService;
import com.lzl.utils.PageUtils;
import ${package}.${moduleName}.entity.${className};

import java.util.Map;

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service extends IBaseService<${className}> {

    PageUtils queryPage( ${className} ${classname},int page, int limit);
}

